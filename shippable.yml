language: java

jdk:
  - openjdk7
  - openjdk8
  - oraclejdk8
 
build:

  ci:
    - java -version
    - echo $SHIPPABLE_JDK_VERSION
   # - mvn --version    
   # - ant -version 
   # - gradle --version 
 
